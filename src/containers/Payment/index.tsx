import React from 'react'


type CheckoutProps = {
    itemIdentity: string[];
    quantity: number;
    address: string;
}

export const Checkout: React.FC<CheckoutProps> = (props) => {
    return (
        <div>
            
        </div>
    )
}