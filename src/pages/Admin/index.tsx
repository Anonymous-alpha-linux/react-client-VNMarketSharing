import React from 'react'

export const AdminDashboard: React.FC<{children?: React.ReactNode}> = ({children}) => {
    return (
        <div>
            <></>
            {children}
        </div>
    )
}